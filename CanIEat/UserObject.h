#import <Foundation/Foundation.h>

@interface UserObject : NSObject {
    NSString *name;
    // username(email)
    // password
    // address
    // history (past orders)
    // location
    // taste or preferences
    // the current order (if any)
}

@end
